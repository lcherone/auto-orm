{
    "name": "autorm",
    "version": "0.0.3",
    "description": "A lightweight easy to use Zero Config MySQL ORM for nodejs that 'automagically' builds your database schema.",
    "main": "src/index.js",
    "scripts": {
        "test": "cd test && ../node_modules/mocha/bin/mocha --exit --check-leaks --slow 500ms",
        "fix": "cd src && standard --fix && standard"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
        "mysql": "^2.15.0"
    },
    "devDependencies": {
        "debug": "^3.1.0",
        "mocha": "^5.1.1",
        "should": "^13.2.3",
        "standard": "^11.0.1"
    },
    "standard": {
        "env": [
            "mocha"
        ]
    }
}